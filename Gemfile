# Local preview only. GitHub Pages builds the published site with its own
# pinned Jekyll, so nothing here is installed on the server.
#
#   bundle install
#   bundle exec jekyll serve   →  http://localhost:4000
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
